// AegisLocation — optional CoreLocation-based location tracking module.
// Declared in Package.swift as an optional target; body is intentionally
// empty until the location-feature work lands. This file exists so SPM
// doesn't fail the target resolution.

import Foundation

public enum AegisLocation {
    public static let version = "0.0.0-stub"
}
